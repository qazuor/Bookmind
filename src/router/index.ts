/**
 * Router barrel export
 *
 * Exports router configuration and utilities.
 */

export { ProtectedRoute, PublicRoute } from "./protected-route";
export { type RoutePath, routePaths, routes } from "./routes";
